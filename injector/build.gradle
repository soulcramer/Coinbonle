plugins {
    id "com.android.library"
    id "kotlin-android"
}

android {
    compileSdk 31

    defaultConfig {
        minSdk 24
    }
}

dependencies {
    implementation project(":data")
    implementation project(":domain")
}
